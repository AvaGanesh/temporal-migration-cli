{
  "name": "temporal-migrate-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "scan": "pnpm --filter @temporal-migrate/cli exec node ./dist/index.js scan"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "@types/node": "^20.14.15"
  }
}
